import { SocialFundPriorityPipe } from './social-fund-priority.pipe';

describe('SocialFundPriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new SocialFundPriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
