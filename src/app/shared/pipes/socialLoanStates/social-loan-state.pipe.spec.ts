import { SocialLoanStatePipe } from './social-loan-state.pipe';

describe('SocialLoanStatePipe', () => {
  it('create an instance', () => {
    const pipe = new SocialLoanStatePipe();
    expect(pipe).toBeTruthy();
  });
});
