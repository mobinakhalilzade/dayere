import { InstallmentStatePipe } from './installment-state.pipe';

describe('InstallmentStatePipe', () => {
  it('create an instance', () => {
    const pipe = new InstallmentStatePipe();
    expect(pipe).toBeTruthy();
  });
});
