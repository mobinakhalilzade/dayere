import { AmountSeparatorPipe } from './amount-separator.pipe';

describe('AmountSeparatorPipe', () => {
  it('create an instance', () => {
    const pipe = new AmountSeparatorPipe();
    expect(pipe).toBeTruthy();
  });
});
